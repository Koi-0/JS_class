// 불리언(Boo lean)

// ------------------------------------------------------------------------------------------------------------------------------------------------------------------ //

// true(참) , false(거짓)
let bool1 = true;
let bool2 = false;

// console.log(bool1);
// console.log(typeof bool1);
// console.log(bool2);
// console.log(typeof bool2);

// ------------------------------------------------------------------------------------------------------------------------------------------------------------------ //

// undefined(un : not / define : 정의하다)
let x;
// console.log(x);

// ------------------------------------------------------------------------------------------------------------------------------------------------------------------ //

// null : 값이 존재하지 않음을 '명시적'으로 나타내는 방법
// 'null'은 개발자가 의도적으로 값이 없다라는 걸 명시하기 위해서 넣은 값이고, 'undefined'는 정의되지 않은 값을 말한다.
let y = null;
console.log(y);

// ------------------------------------------------------------------------------------------------------------------------------------------------------------------ //

// object(객체) : key-value pair
let person = {
    name: "choi",
    age: 20,
    isMarried: true,
};

console.log(typeof person);

// ------------------------------------------------------------------------------------------------------------------------------------------------------------------ //

// array(배열) : 여러 개의 데이터를 순서대로 저장하는 데이터 타입이다.
let number = [1, 2, 3, 4, 5];
let fruits = ["apple", "banana", "orange"];

// ------------------------------------------------------------------------------------------------------------------------------------------------------------------ //
