// 스코프, 전역변수, 지역변수, 화살표함수
// 스코프 : 변수의 영향 범위
// 스코프가 전체의 영역에서 영향을 끼칠 수 있으면 전역변수, 함수 내에서만 영향을 끼칠 수 있으면 지역변수라 할 수 있다.

// ------------------------------------------------------------------------------------------------------------------------------------------------------------------ //

// 지역변수
function prinX() {
    let x = 10;

    console.log(x);
}

console.log(x);
prinX();

// ------------------------------------------------------------------------------------------------------------------------------------------------------------------ //
