// obj
// 2가지 속성을 가짐 : (vals 라는 속성) + (logValues 라는 함수)

// ------------------------------------------------------------------------------------------------------------------------------------------------------------------ //

var obj = {
    vals: [1, 2, 3],
    logValues: function (v, i) {
        console.log("test starts");
        if (this === global) {
            console.log("this는 glabal입니다.");
        } else {
            console.log(this, v, i);
        }
        console.log("test end");
    },
};

// method로써 호출
// obj.logValues(1, 2);

// ------------------------------------------------------------------------------------------------------------------------------------------------------------------ //

// callback -> obj를 this로 하는 메서드를 그대로 전달한 게 아니다.
// 단지, obj.logValues가 가리키는 함수만 전달한 것이다.(obj 객체와는 연관이 없다.)

// ------------------------------------------------------------------------------------------------------------------------------------------------------------------ //

// forEach, map, filter
[4, 5, 6].forEach(obj.logValues);

// ------------------------------------------------------------------------------------------------------------------------------------------------------------------ //
